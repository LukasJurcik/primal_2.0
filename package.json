{
  "name": "primal-scripts",
  "version": "1.0.0",
  "scripts": {
    "build": "terser main.js -o main.min.js -c drop_console=true -m",
    "watch": "nodemon --watch main.js --exec 'npm run build'"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "terser": "^5.44.1"
  }
}

